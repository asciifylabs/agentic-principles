interface:
  display_name: "Docker Principles"
  short_description: "Docker build and runtime standards"
  default_prompt: "Use $docker-principles to apply the relevant engineering standards to this task."
policy:
  allow_implicit_invocation: true
